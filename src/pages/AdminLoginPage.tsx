import { supabase } from '../lib/supabaseClient';import { useState } from 'react';
export function AdminLoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');async function login(e:React.FormEvent){e.preventDefault();const{error}=await supabase.auth.signInWithPassword({email,password}); if(error) alert(error.message); else location.href='/admin';}return <main className="grid min-h-screen place-items-center bg-navy"><form onSubmit={login} className="w-full max-w-sm rounded-2xl bg-white p-8"><h1 className="text-3xl font-black">Admin Login</h1><input className="mt-6 w-full rounded border p-3" placeholder="email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="mt-3 w-full rounded border p-3" type="password" placeholder="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="mt-5 w-full rounded bg-cyan p-3 font-bold text-navy">Entra</button></form></main>}
