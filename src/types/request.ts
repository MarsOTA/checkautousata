export type RequestStatus = 'new'|'contacted'|'scheduled'|'completed'|'cancelled'|'archived';
export interface InspectionRequest { id?: string; created_at?: string; updated_at?: string; customer_name: string; customer_email: string; customer_phone: string; car_brand: string; car_model: string; car_year: string; car_plate?: string | null; seller_city: string; seller_address: string; preferred_date?: string | null; preferred_time?: string | null; service_type: string; message?: string | null; status?: RequestStatus; admin_notes?: string | null; source?: string | null; privacy_accepted: boolean; marketing_accepted?: boolean; }
