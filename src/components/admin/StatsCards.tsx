import type { InspectionRequest } from '../../types/request';
export function StatsCards({rows}:{rows:InspectionRequest[]}){const stats=[['Totali',rows.length],['Nuove',rows.filter(r=>r.status==='new'||!r.status).length],['Contattate',rows.filter(r=>r.status==='contacted').length],['Completate',rows.filter(r=>r.status==='completed').length]];return <div className="grid gap-4 md:grid-cols-4">{stats.map(([k,v])=><div className="rounded-2xl bg-white p-6 shadow-sm" key={k as string}><div className="text-sm text-muted">{k}</div><div className="mt-2 text-3xl font-black text-ink">{v}</div></div>)}</div>}
